version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - echo Installing dependencies...

  pre_build:
    commands:
      - echo Preparando ambiente...

  build:
    commands:
      - echo "Iniciando build do react..."
      - cd client
      - npm install
      - NODE_OPTIONS=--openssl-legacy-provider REACT_APP_API_URL=https://02iohfnyh8.execute-api.us-east-1.amazonaws.com/Prod SKIP_PREFLIGHT_CHECK=true npm run build

artifacts:
  base-directory: client/build
  files:
    - '**/*'
